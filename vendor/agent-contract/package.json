{
  "name": "@agent-network/contract",
  "version": "0.1.0",
  "private": true,
  "description": "Shared agent metadata + run-summary contract for the Soofi Agent Network. The integration boundary between registrable agents and the registry.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "yaml": "^2.5.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "vitest": "^2.1.5"
  }
}
