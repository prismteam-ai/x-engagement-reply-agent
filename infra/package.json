{
  "name": "x-engagement-reply-agent-infra",
  "version": "0.1.0",
  "description": "CDK app for the X Engagement Reply Agent (DynamoDB + scheduled Lambda).",
  "private": true,
  "bin": {
    "infra": "bin/app.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "tsc -p tsconfig.json",
    "synth": "cdk synth",
    "diff": "cdk diff",
    "deploy": "cdk deploy",
    "destroy": "cdk destroy"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.180.0",
    "constructs": "^10.4.2"
  },
  "devDependencies": {
    "@types/node": "^22.10.7",
    "aws-cdk": "^2.180.0",
    "esbuild": "^0.24.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.3"
  }
}
